HW1-1
先完全用迴圈寫階層當N小於等於一時就結束，再用遞迴寫階層，遞迴終止條件為N<=1時，用無限WHILE迴圈不斷的去CALL，當輸入-1時才結束程式

HW1-2
一開始先用字串讀入輸入數字且去除掉{，當第一個陣列字元為}時就直接印出結果然後結束，再判斷陣列中的'，''}'，下去做陣列的分配
再用FOR迴圈去重複CALL遞迴函數，此函數有三個引數，(位置，FLAG數量，此回合印幾次)，終止條件為位置比陣列長度多時，以及FLAG數
量跟此回合應該列印幾次的數量一樣，當一樣時就，CALL PrintAns將答案列印出來，而且如果有立起FLAG的將會列印出，沒有的則否。